name: tests

on:
  push:
  pull_request:

# Нужно для деплоя Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Чтобы новые деплои отменяли предыдущие
concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Install Playwright browsers
        run: python -m playwright install --with-deps

      # Запускаем тесты (HTML и Allure сырые результаты создаются за счёт pytest.ini)
      - name: Run tests (UI + API)
        run: |
          pytest -q --tracing=retain-on-failure --video=retain-on-failure
        env:
          PYTHONWARNINGS: ignore

      # Генерим статический Allure из raw результатов
      - name: Generate Allure Report
        uses: simple-elf/allure-report-action@master
        with:
          allure_results: reports/allure-results
          allure_report: reports/allure-report

      # Сохраняем HTML-репорт как артефакт (удобно скачать из Actions)
      - name: Upload HTML report (artifact)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: html-report
          path: reports/html/report.html

      # Сохраняем сырые Allure результаты (на всякий случай)
      - name: Upload Allure results (artifact)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-results
          path: reports/allure-results

      # Готовим артефакт для GitHub Pages (именно эта строка делает сайт)
      - name: Upload Pages artifact (Allure site)
        if: always()
        uses: actions/upload-pages-artifact@v3
        with:
          path: reports/allure-report

  deploy:
    # Деплой стартует только после сборки
    needs: build-and-test
    runs-on: ubuntu-latest
    if: always()
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      # Деплой артефакта на GitHub Pages
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
